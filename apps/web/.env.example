# WorkOS AuthKit Configuration
# Get these from: https://dashboard.workos.com

# API Key from WorkOS Dashboard → API Keys
WORKOS_API_KEY=sk_test_...

# Client ID from WorkOS Dashboard → Project Settings
WORKOS_CLIENT_ID=client_...

# Cookie password - generate a 32+ character secret
# Use: openssl rand -base64 32
WORKOS_COOKIE_PASSWORD=your-32-char-secret-here

# Redirect URI - must match WorkOS Dashboard → Redirect URIs config
# For local dev: http://localhost:3000/auth/callback
# For production: https://your-domain.com/auth/callback
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback

# Client-readable redirect URI (same as above)
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback
