---
phase: 01-authentication-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
autonomous: true

must_haves:
  truths:
    - "Auth web app can be launched from repo root using workspace scripts"
    - "Auth web app dev server can run locally once scaffolded"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package globs"
    - path: "package.json"
      provides: "Root workspace config and scripts"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "apps/*"
      via: "workspace inclusion"
      pattern: "apps/\\*"
    - from: "package.json"
      to: "apps/web"
      via: "root scripts using pnpm -C apps/web"
      pattern: "apps/web"
---

<objective>
Bootstrap the pnpm workspace foundation for Phase 1 auth.

Purpose: Establish the monorepo root configuration before creating the web app.
Output: Root package.json and pnpm workspace config.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authentication-access/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root package.json for workspace scripts</name>
  <files>package.json</files>
  <action>
    - Create a root package.json marked private with pnpm workspace scripts (e.g., dev/build/lint delegating to apps/web via pnpm -C apps/web).
    - Keep scripts minimal and aligned to apps/web to avoid ambiguity later.
  </action>
  <verify>pnpm install</verify>
  <done>Root package.json exists with pnpm workspace scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Add pnpm workspace configuration</name>
  <files>pnpm-workspace.yaml</files>
  <action>
    - Create pnpm-workspace.yaml with workspace globs: apps/* and packages/* (for future backend/ui packages).
  </action>
  <verify>pnpm install</verify>
  <done>Workspace config includes apps/* and packages/* globs.</done>
</task>

</tasks>

<verification>
- pnpm install completes at repo root
</verification>

<success_criteria>
- Workspace is ready for additional packages
- Root scripts target apps/web consistently
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-access/01-01-SUMMARY.md`
</output>
