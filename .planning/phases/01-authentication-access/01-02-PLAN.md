---
phase: 01-authentication-access
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/web/package.json
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.js
autonomous: true

must_haves:
  truths:
    - "Users can load the web app home page (pre-auth UI)"
    - "Home page renders without runtime errors when running locally"
  artifacts:
    - path: "apps/web/app/layout.tsx"
      provides: "App Router root layout"
    - path: "apps/web/app/page.tsx"
      provides: "Home page entry"
    - path: "apps/web/package.json"
      provides: "Next.js app dependencies"
    - path: "apps/web/next.config.ts"
      provides: "Next.js config"
  key_links:
    - from: "apps/web/package.json"
      to: "apps/web/app/page.tsx"
      via: "Next.js App Router defaults"
      pattern: "app/page.tsx"
---

<objective>
Generate the Next.js App Router app in apps/web.

Purpose: Establish the web app foundation required for AuthKit integration.
Output: apps/web Next.js app with App Router configuration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authentication-access/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Next.js App Router app in apps/web</name>
  <files>apps/web/package.json, apps/web/app/layout.tsx, apps/web/app/page.tsx</files>
  <action>
    - Run pnpm create next-app in apps/web with TypeScript, Tailwind, ESLint, and App Router enabled (no /src directory).
    - Ensure app/page.tsx and app/layout.tsx exist as server components with default content.
  </action>
  <verify>pnpm install</verify>
  <done>apps/web contains a Next.js App Router app scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm base app builds cleanly</name>
  <files>apps/web/app/page.tsx, apps/web/app/layout.tsx</files>
  <action>
    - Ensure the default home page renders without runtime errors (no placeholder TODOs or missing imports).
    - Keep the page minimal; no auth wiring yet in this plan.
  </action>
  <verify>pnpm -C apps/web build</verify>
  <done>Next.js build succeeds for apps/web.</done>
</task>

</tasks>

<verification>
- pnpm install completes at repo root
- pnpm -C apps/web build completes successfully
</verification>

<success_criteria>
- apps/web App Router project builds successfully
- Default home page renders without runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-access/01-02-SUMMARY.md`
</output>
